//% color="#6b5b95" icon="\uf00a"
namespace tilemapStamp {
    /**
     * A trivial test block used only to check whether custom extension
     * categories are showing up in the toolbox at all.
     */
    //% block
    export function testBlock(): void {
        game.splash("It works!");
    }
}
